Cosas que faltan Usuarios:
-Mis Pedidos
-Logica de favoritos

Cosas que faltan Empresas:
-Pedidos en Curso
-Resumen

Cosas que faltan General:
-Agregar mas informacion e imagenes en el apartado de Informacion
-Base de datos
-De las barras laterales arreglar que te lleve a cada cosa bien
-Recupero de contrasena cuando te redirige el mail


Sidebar

        .sidebar {      /* cuadro blanco de la sidebar */
            height: 100vh;
            background-color: white;
            width: 16rem;
            padding: 1.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            position: fixed;
        }
        .main-content {
            margin-left: 2rem; /* creo y lo agrego al div */
            flex: 0.13;
            padding: 1rem;
        }
        .nav-item {
            margin: 2rem 0;
            display: flex;
            align-items: center;
            color: #6b7280;
            transition: color 0.3s ease;
        }
        .nav-item i {
            height: 24px;
            width: 24px;
            margin-right: 0.5rem;
        }
        .nav-item a {
            color: inherit;
            text-decoration: none;
        }
        .nav-item:hover,
        .nav-item.active {
            color: #27a2e8;
        }
        

editar usurio abajo

<div class="profile mt-auto">
            <img src="{{ url_for('static', filename='uploads/' ~ user_image) }}" alt="Profile">
            <div class="profile-text">
                <div class="name">{{ user_name }}</div>
                <a href="{{ url_for('perfil_usuario') }}" class="edit-profile">Edit Profile</a>
            </div>
        </div>

        
        css

                .profile img {
            height: 40px;
            width: 40px;
            border-radius: 50%;
            margin-right: 0.5rem;
        }
        .profile-text {
            display: flex;
            flex-direction: column;
        }
        .profile-text .name {
            font-weight: bold;
        }
        .profile-text .edit-profile {
            color: #6b7280;
            font-size: 0.875rem;
        }